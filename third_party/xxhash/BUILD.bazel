# The actual BUILD file for the external `@xxhash_reference` repo is
# xxhash.BUILD.bazel (deliberately not named BUILD.bazel at repo paths that
# Bazel would load as a mboworks_mbo package).
exports_files(["xxhash.BUILD.bazel"])
